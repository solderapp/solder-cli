workspace:
  base: /srv/app
  path: src/github.com/kleister/kleister-cli

pipeline:
  testing:
    image: webhippie/golang:latest
    pull: true
    environment:
      - GOPATH=/srv/app
    commands:
      - make clean
      - make deps
      - make vet
      - make lint
      - make test
      - make build

  updater:
    image: webhippie/golang:latest
    environment:
      - GOPATH=/srv/app
    commands:
      - make publish
    when:
      event: [ push, tag ]
      branch: [ master, refs/tags/* ]

  coverage:
    image: kleister/coverage
    server: http://coverage.webhippie.de

  # rsync:
  #   image: plugins/rsync
  #   user: deploy
  #   host: dl.webhippie.de
  #   source: dist/release/
  #   target: /usr/share/webapps/dl/public_html/kleister-cli/master
  #   recursive: true
  #   delete: true
  #   when:
  #     event: push
  #     branch: master

  # rsync:
  #   image: plugins/rsync
  #   user: deploy
  #   host: dl.webhippie.de
  #   source: dist/release/
  #   target: /usr/share/webapps/dl/public_html/kleister-cli/$$TAG
  #   recursive: true
  #   delete: true
  #   when:
  #     event: tag
  #     branch: refs/tags/*

  # rsync:
  #   image: plugins/rsync
  #   user: deploy
  #   host: dl.webhippie.de
  #   source: dist/latest/
  #   target: /usr/share/webapps/dl/public_html/kleister-cli/latest
  #   recursive: true
  #   delete: true
  #   when:
  #     event: tag
  #     branch: refs/tags/*

  # rsync:
  #   image: plugins/rsync
  #   user: deploy
  #   host: dl.webhippie.de
  #   source: dist/publish/
  #   target: /usr/share/webapps/dl/public_html/kleister-cli/updates
  #   recursive: true
  #   delete: false
  #   when:
  #     event: tag
  #     branch: refs/tags/*

  # github_release:
  #   image: plugins/github-release
  #   files:
  #     - dist/release/*
  #   when:
  #     event: tag
  #     branch: refs/tags/*

  docker:
    image: plugins/docker
    storage_driver: overlay
    debug: true
    repo: kleister/kleister-cli
    tags: [ '${TAG}' ]
    when:
      event: tag
      branch: refs/tags/*

  docker:
    image: plugins/docker
    storage_driver: overlay
    debug: true
    repo: kleister/kleister-cli
    tags: [ 'latest' ]
    when:
      event: push
      branch: master

  gitter:
    image: kleister/gitter
